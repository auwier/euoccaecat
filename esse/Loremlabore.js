// Initialize an array to hold parent objects
let allParents = [];

// Define a parent object
let parent = {
    name: "John Doe",
    age: 40,
    children: ["Jane", "Jack"]
};

// Use the push method to add the parent object to the array
allParents.push(parent);

// Display the contents of the allParents array
console.log(allParents);
